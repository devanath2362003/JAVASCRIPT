let today = new Date()

console.log(today);

console.log(today.getDate());
console.log(today.getDay());
console.log(today.getFullYear());
console.log(today.getMilliseconds());
console.log(today.getTime());
console.log(today.toLocaleTimeString());
console.log(today.toLocaleDateString());



// 1. Display the Current Date 
//    - Retrieve and show the current date and time from the system clock.

// 2. **Format a Date**  
//    - Convert a date object into a human-readable format (e.g., `DD/MM/YYYY`).

// 3. **Parse a Date String**  
//    - Convert a date string into a valid date object for further manipulation.

// 4. **Compare Two Dates**  
//    - Check if two dates are equal or determine which one comes earlier or later.

// 5. **Add/Subtract Days from a Date**  
//    - Modify a date by adding or subtracting a specific number of days.

// 6. **Calculate the Difference Between Two Dates**  
//    - Find the number of days, hours, or other units between two dates.

// 7. Time of the Day:
// 	 Create a program that takes the current hour as input and prints out a message indicating whether it's 	morning, afternoon, evening, or night.

// 8. Age Calculator


